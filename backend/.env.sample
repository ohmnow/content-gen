# Content Generation Backend Environment Variables

# Required: OpenAI API Key for Sora video generation
OPENAI_API_KEY=

# Optional: Video storage path (defaults to ./videos)
VIDEO_STORAGE_PATH=./videos

# Optional: Maximum poll timeout in seconds (default: 600)
MAX_POLL_TIMEOUT=600

# Optional: Default Sora model (default: sora-2)
DEFAULT_MODEL=sora-2

# Optional: Default video size (default: 1280x720)
DEFAULT_SIZE=1280x720

# Optional: Default video duration in seconds (default: 4)
DEFAULT_SECONDS=4

# Optional: Maximum file size in bytes (default: 10485760 / 10MB)
MAX_FILE_SIZE=10485760
